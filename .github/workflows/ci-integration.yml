name: CI - Integration (Docker Compose Stack)

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

concurrency:
  group: ci-integration-${{ github.ref }}
  cancel-in-progress: true

jobs:
  integration:
    name: Stack Smoke Test
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4

      - name: Build stack
        run: docker compose build --parallel

      - name: Start stack
        run: docker compose up -d

      - name: Wait for backend health
        run: |
          for i in $(seq 1 30); do
            STATUS=$(curl -sf http://localhost:8080/health | jq -r '.status' 2>/dev/null || echo "")
            [ "$STATUS" = "ok" ] && { echo "Backend healthy"; break; }
            echo "Attempt $i - waiting..."; sleep 3
          done
          [ "$STATUS" = "ok" ] || { docker compose logs backend; exit 1; }

      - name: Wait for frontend health
        run: |
          for i in $(seq 1 20); do
            curl -sf http://localhost:3000 && { echo "Frontend healthy"; break; }
            echo "Attempt $i - waiting..."; sleep 3
          done

      - name: Run API smoke tests
        run: |
          curl -sf http://localhost:8080/health
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:8080/api/v1/ledger/ping)
          [ "$STATUS" = "200" ] || [ "$STATUS" = "401" ] || \
            { echo "Unexpected ledger endpoint status: $STATUS"; exit 1; }

      - name: Run RBAC API compatibility checks
        run: |
          USERS_STATUS=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:8080/api/v1/users)
          ROLES_STATUS=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:8080/api/v1/roles)
          PERMISSIONS_STATUS=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:8080/api/v1/permissions)

          [ "$USERS_STATUS" = "401" ] || { echo "Expected /users to be 401 unauthenticated, got: $USERS_STATUS"; exit 1; }
          [ "$ROLES_STATUS" = "401" ] || { echo "Expected /roles to be 401 unauthenticated, got: $ROLES_STATUS"; exit 1; }
          [ "$PERMISSIONS_STATUS" = "401" ] || { echo "Expected /permissions to be 401 unauthenticated, got: $PERMISSIONS_STATUS"; exit 1; }

      - name: Run frontend RBAC route compatibility checks
        run: |
          USERS_PAGE_STATUS=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/users)
          ROLES_PAGE_STATUS=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/roles)
          PERMISSIONS_PAGE_STATUS=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/permissions)

          [ "$USERS_PAGE_STATUS" = "200" ] || { echo "Expected /users page to render, got: $USERS_PAGE_STATUS"; exit 1; }
          [ "$ROLES_PAGE_STATUS" = "200" ] || { echo "Expected /roles page to render, got: $ROLES_PAGE_STATUS"; exit 1; }
          [ "$PERMISSIONS_PAGE_STATUS" = "200" ] || { echo "Expected /permissions page to render, got: $PERMISSIONS_PAGE_STATUS"; exit 1; }

      - name: Capture compose logs
        if: always()
        run: |
          mkdir -p /tmp/compose-logs
          docker compose logs --no-color > /tmp/compose-logs/compose.log || true

      - name: Tear down
        if: always()
        run: docker compose down -v

      - name: Upload compose logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: compose-logs
          path: /tmp/compose-logs/
